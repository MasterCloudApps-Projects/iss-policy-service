pricing-client:
  enabled: true
  
spring:
  jackson.serialization.indent_output: true
  application:
    name: iss-policy-service
  cloud:
    kubernetes:  
      discovery:
          enabled: false

